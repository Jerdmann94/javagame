package com.models;

import com.badlogic.gdx.scenes.scene2d.Event;
import com.badlogic.gdx.scenes.scene2d.EventListener;

public class SetTarget implements EventListener {
    @Override
    public boolean handle(Event event) {
        return false;
    }
}
